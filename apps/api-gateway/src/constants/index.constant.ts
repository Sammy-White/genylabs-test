export enum Roles {
  admin = 'admin',
  provider = 'provider',
}

export enum Services {
  manicuse = 'MANICURE',
  pedicure = 'PEDICURE',
  haircut = 'HAIRCUT',
}

export enum APP_SERVICES {
  BOOKING = 'BOOKING',
  USER = 'USER',
  NOTIFICATION = 'NOTIFICATION',
}
